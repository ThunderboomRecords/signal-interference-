import { Song } from "./project";

export function selectSong(song: Song) {
  // Select song in the project.
  // Should add on recording callback
  // should stop recording before switching if it is a different song
  //
}
